
// DO NOT EDIT THIS FILE! IT IS GENERATED EACH TIME "capacitor update" IS RUN

android {
  compileOptions {
      sourceCompatibility JavaVersion.VERSION_17
      targetCompatibility JavaVersion.VERSION_17
  }
}

apply from: "../capacitor-cordova-android-plugins/cordova.variables.gradle"
dependencies {
    implementation project(':capacitor-camera')
    implementation project(':capacitor-device')
    implementation project(':capacitor-filesystem')
    implementation project(':whiteguru-capacitor-plugin-media-capture')
    implementation "androidx.webkit:webkit:1.4.0"
}

// Forzar Java 17 para este módulo usando la forma correcta (sin options.release)
tasks.withType(JavaCompile).configureEach {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
    // Eliminamos la línea options.release.set(17) que estaba causando el error
}

if (hasProperty('postBuildExtras')) {
  postBuildExtras()
}
